let tool = '';

tool = 'Bolígrafo'

// Use short circuit evaluation to assign  writingUtensil variable below:
let writingUtensil = tool || 'Lápiz'

console.log(`El ${writingUtensil} es mas poderoso que la espada.`);